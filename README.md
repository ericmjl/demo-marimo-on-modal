# Demo for modal deployment of Marimo notebooks

To deploy the app, run:

```bash
modal deploy deployment.py
```

To run the notebook locally, run:

```bash
uvx marimo edit --sandbox protein_estimation.py
```
